# Build output
/.dist/
/lwd
/lwd-web
/lwd-mcp
/lwd-agent

# Local runtime data (if a dev points LWD_DATA_DIR here)
/var/
*.db
*.db-journal
secret.key

# Superpowers SDD working dir (task briefs, reports, review diffs, ledgers)
/.superpowers/
